server:
  port: ${PORT:8080}

spring:
  application:
    name: monitoring-admin

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true

app:
  monitoring:
    project-id: ${GCP_PROJECT_ID}
