# Branching Conflict Demo

This directory is a small sandbox used to demonstrate two pull request flows:

- sibling branches opened from the same base branch
- chained branches where each branch builds on the previous one

The files here are intentionally simple so merge conflicts are easy to inspect.
